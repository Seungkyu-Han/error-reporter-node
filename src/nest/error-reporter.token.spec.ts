import { ERROR_REPORTER_OPTIONS } from './error-reporter.tokens';

describe('ERROR_REPORTER_OPTIONS', () => {
    it('should be defined', () => {
        expect(ERROR_REPORTER_OPTIONS).toBeDefined();
    });
});
